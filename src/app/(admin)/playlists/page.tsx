import { PlaylistsPageClient } from "@/components/playlists/playlists-page-client";

export const metadata = {
  title: "Playlists",
};

export default function PlaylistsPage() {
  return <PlaylistsPageClient />;
}
